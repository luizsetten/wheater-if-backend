version: "3.9"

services:
  database:
    image: postgres
    container_name: wheather_database
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=wheater
      - POSTGRES_PASSWORD=umbelodiaparaestudarnoif
      - POSTGRES_DB=wheater
    volumes:
      - pgdata:/data/postgres

volumes:
  pgdata:
    driver: local
